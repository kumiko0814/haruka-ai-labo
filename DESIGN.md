# AIラボ：画面デザインの引き継ぎ

2026-09-24 に全12ページを共通のデザインへ更新しました。

- 共通スタイルは `ui-studio.css`、画面の補助操作は `ui-studio.js` です。
- `ui-refined.js` はキーボード操作・ダイアログのフォーカス・目次などの既存機能を維持しています。
- `ui-refined.css` と `ui-atelier.css/js` は現行ページから読み込みません。過去版の参照として残っています。
- 色はアイボリー `#f6f7f3`、深いグリーン `#193c36`、ライム `#daedaa`。本文は Noto Sans JP と端末の日本語フォントです。
- 装飾を追加するよりも、見出し・本文・入力欄・補足の優先順位と余白をそろえてください。
- フォーム・主要操作・エラーは色だけに頼らず、文章とフォーカスでも伝えてください。
- `start.js` のメールアドレスから会員IDを作る処理は既存のものと互換です。アルゴリズムを変えると以前のカルテと接続しなくなるため、認証移行とは別作業として扱ってください。
- 設定は `labo_config.js`、データ接続は `supabase_config.js` にあります。デザイン修正でこれらを重複定義しないでください。
- 事例ページは元の返信を折りたたみに保持しています。新しい比較画像はAI制作の説明用サンプルです。現行カルテが画像を自動生成する機能を持つという表示にはしないでください。
- UIの確認は `?demo=1`、またはデータ通信を無効にしたローカル環境で行い、受講生の相談記録へテスト送信しないでください。
- モバイル375pxとデスクトップ1440pxで確認し、入力、メニュー、カルテの6画面、事例の開閉を確認してください。

Claudeで機能を追加した後も、この共通CSSを基準にCodexで画面を整えられます。同じブランチを同時に書き換えず、GitHubの最新状態を取得してから変更を進めます。
